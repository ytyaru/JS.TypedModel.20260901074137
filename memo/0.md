# TypedModel

* https://github.com/ytyaru/JS.Typ.20260823112517

これを使い型判定するプロパティを持つオブジェクトを生成する。

```javascript
const human = model({
  name: isT.p.str,
  age: isT.p.int,
  items: isT.o.ary,
  children: {
    sub: isT.o.obj,
  }
});
```

```
Object.defineProperty(o, name, {get(){} set(v){}})
```

```
structure-object
class-instance
model-entity
template-document
```

API|概要
---|----
`dfv(v)`|`v`の型を取得し適切な初期値を返す。
`struct({...})`|指定したプロパティが指定した型であることを代入する度に判定する。
`model({...})`|``

定型。既存プロパティに値を代入できる。存在しないプロパティを代入しても追加されない。既存プロパティを削除できない。`Object.seal()`。

```javascript
const human = struct({
  name: isT.p.str,
  age: isT.p.int,
  items: isT.o.ary,
  children: {
    sub: isT.o.obj,
  }
});
```

定数。既存プロパティに値を代入できない。存在しないプロパティを代入しても追加されない。既存プロパティを削除できない。`Object.freeze()`。

```javascript
const human = struct.freeze({
  name: isT.p.str,
  age: isT.p.int,
  items: isT.o.ary,
  children: {
    sub: isT.o.obj,
  }
});
```







よし。次はマージ。既存オブジェクトに新しいbaseプロパティやディスクリプタ（アクセサ）を追加する。
以前`struct(o,{...})`のように第二引数まである時にマージすると言ったが、そのインタフェースは不味い気がしてきた。責任混在だ。

それと既存sealオブジェクトに対して追加することが技術的に可能かどうかもわからない。仮に可能だとして、そうすべきかは状況次第な気がする。新しいオブジェクトを作って欲しい場合もあるはずだ。

ならAPIは二つ用意すべきなのでは？

既存オブジェクトに追加する版（技術的に可能か不明）
```javascript
struct.merge(human, {
  addProp: isT.p.int,
});
```

新しいオブジェクトを作成して返却する版（多分こっちは技術的に可能）
```javascript
const humanEx = struct.clone(human, {
  addProp: isT.p.int,
});
```

それと追加でなく削除するとか改名したい時はどうしたらいいのか。もう最初から作り直せよ、というべきはどのラインなのか。

そもそもこのマージだコピーだは本当に必要か？　シャローコピーなら簡単にできるはずだしスプレッド展開すればマージは簡単にできるのでは？　以下でシャローコピーなマージならできそうだ。

```
const a = struct({
  name: isT.p.str,
});
const b = struct({
  age: isT.p.int,
});
const c = {...a, ...b};
```

ただ、シャローコピーだと困る。別のインスタンスとして扱いたい場合がある、というかそれが基本のはずだ。よってシャローコピーでは困る。特にスキーマでオブジェクト化された部分は恐らくシャローコピーにより全部共通のメモリになってしまい独立したメモリを持てない状態になってしまうだろう。それは困る。

`clone`に関してはどうするか。`structuredClone()`ではディスクリプタはコピーできないのでは？なら`clone`APIは自前で実装する必要がありそうだ。

同じ構造にしたいけど、値は別のメモリで保存させたい。そういう要件はあるはずだ。classとinstanceみたいなことをするメソッドが必要だ。

だとしたらマージする時もcloneを使うべきだろうか。いやマージという言葉がなんか違う。`extends`拡張にするか。

```
const human1 = struct({name: owT.p.str});
struct.extends(human, {age:owT.p.int});
```
```
const human2 = struct.clone(human1);
struct.extends(human, {level:owT.p.big});
```

しかしこれはどうなんだ？　仮に技術的にextendsできるとして。それを禁じるのがsealのはず。でもやりたい場合もある。どうしたらいい？

ベースは変更せず、必ず新規作成するようにしたらいいのでは？内部でcloneし、それに第二引数を付け加えるように新規作成すればいいのでは？

```
const human = struct({name: owT.p.str});
const humanEx = struct.extends(human, {age:owT.p.int});
```
```
const humanExIns1 = struct.clone(humanEx);
const humanExIns2 = struct.clone(humanEx);
const humanExIns3 = struct.clone(humanEx);
humanExIns1.name = 'A';
humanExIns2.name = 'B';
humanExIns3.name = 'C';
```

この場合、クラスでいう`new`と同じ意味である`struct()`/`struct.freeze()`と、メソッドの意味合いである`struct.extends()`/`struct.clone()`の区別が付きづらい。

```
class Struct {
    constructor(o, isFreeze) {
    }
    clone() {}
    extends() {}
}
```

上記のようにクラスにすべきか？でもクラスにすると返却値がプレーンなオブジェクトではなくなってしまう。メソッドをぶら下げると、その名前のプロパティを持てなくなってしまう。それは不味い。

だからやはり従来通り、関数オブジェクト方式で実装することになる。

では、コンストラクタとメソッドの見分けが付かない問題をどうするか。

1. 無視する。混同したままで問題ない。覚えて使い分ければいい（でも`struct.freeze({...})`に既存オブジェクトを代入するという間違った使い方をしちゃうかも。その時のエラーメッセージも分かりにくい）
2. `struct.new()`,`struct.new.freeze()`のように改名する（見分けは付くが長くなる）
3. `struct({...}, isFreeze)`のように第二引数で区別する（コード上で`freeze`が明記されないので意味が分かりにくい。文字リテラルにするとタイポする可能性があるし自動補完もできない）
4. `struct.seal()`, `struct.freeze()`に改名する（ただ`struct()`が長くなっただけでコンストラクタかメソッドかの見分けが付かないメソッドが増えただけ）

とりあえず1でいいか。

`clone`と`extends`の2メソッドを追加することは可能か？

上記の場合、`human2`には`level`があるけど`human1`にはない。

```
const human2 = struct.clone(human);
```





スキーマ定義の保存が必要か。確かに。インスタンス化に相当するcloneを作るなら、スキーマ定義の使い捨てができなくなるわけか。

だとすると返却されるオブジェクトは、スキーマ定義と状態の二種類を持つ必要があるわけだが。メモリ効率と自由度を考えて最適化するためには、どのように保持すべきか考える必要がありそうだ。

* スキーマ定義は最初に渡されたものをそのままシャローコピーで持ち続けcloneしない。例えcloneメソッドで呼び出されてもシャローコピーする（スキーマ定義は名前と関数だけだから、もとより自然とシャローコピーになるはずだが、それは関数部分のみ。オブジェクト自体は新規作成することになるのか？）
* 状態はcloneされたらcloneする。

このような違いが生まれるはずだ。

ところで現状、状態を`_`という内部プロパティで持っている。しかしお前が言うようにSymbolを使ったほうが良い気がする。スキーマ定義と状態はそれぞれシンボルで持ち、そこにオブジェクトを値として代入する。プロパティにはプロパティ名を持たせる。同じ名前からスキーマ定義と状態を取得できるようにする。

これにて`_`という名前もエンドユーザが使えるようになる。

ただ私はSymbolをあまり使ったことがない。確か呼び出す度に異なる一意の値になるはず。でもそれだと困る。ルートで二つのsymbolを作成し内部でメモリ保持して使いまわすか。それとも`Symbol.for()`を使うか。多分前者のほうが文字リテラルを使わずに済んで良い。Symbol.forだと重複の可能性もありえるし避けるのが無難だろう。

APIで問題なのは`struct.freeze()`だ。これはコンストラクタ的用途をするものだが、メソッド的に見える。それが問題なのだ。`struct()`はコンストラクタ的に見えるから問題ない。`struct.clone()`,`struct.extends()`はメソッド的に見えるから問題ない。問題は`struct.freeze()`だ。コンストラクタ的なものなのに、メソッド的に見えてしまう。それが問題だと言っているのだ。お前は私の言っている意味が分かっていないようだな。これは私が示した4つの方法のいずれかしかなく、そのどれも問題がある。現状は特に何もしない1で妥協する。それはつまり`struct.freeze()`がメソッドに見える状態のままのAPIでいくということだ。何の解決もできやしない。お前は見当違いな話をしている。どうせこう話しても理解できやしないだろう。やれやれだ。





以下のコードだが、文字リテラルに意味あるの？ないよね？

```
// モジュールスコープで一意なメタデータ用シンボル（ユーザー空間を汚染しない）
const kSchema = Symbol('struct.schema');
const kState = Symbol('struct.state');
```

あと、私はパフォーマンス改善として以下のようにコードの記述を調整して字数を減らした。minifyをかけた時に短くなるような記述に調整してある。`isSchema`を`isT.o.obj`に変えたり、`if`文を三項演算子にして`return`文字列を減らしたりしてある。このコードをベースに、clone()とextends()を追加したコードを書いてくれ。

お前は勝手に記法を変えようとするかもしれないが、それは辞めろ。このソースコードは繊細なチューニングをしてある。可能な限り守れ。

## struct.js

```javascript
import { isT, owT, tof } from '../../lib/ist/dist/bun/esm/bundle.js';
import {dfv} from './dfv.js';

const getDefaultValue = (validator) => isT.o.obj(validator) ? makeBaseObj(validator) : dfv(validator);

const makeBaseObj = (o) => {
    const base = {};
    for (let [name, validator] of Object.entries(o)) {
        base[name] = getDefaultValue(validator);
    }
    return base;
};

// 1. 各種セッターの生成（初期化時に完全に独立した関数として生成し、実行時分岐を排除）
const createFreezeSetter = () => () => {
    throw new Error('Cannot assign to a frozen struct.');
};

const createPrimitiveSetter = (baseData, name, validator) => (v) => {
    if (validator(v)) {
        baseData[name] = v;
    }
};

const createSealUndefinedHandler = (k) => {
    throw new Error(`Property "${k}" is not defined in the schema.`);
};

// アトミック性のための事前検証
const validateNestedAssignment = (validator, v) => {
    owT.o.obj(v);
    const schemaKeys = Object.keys(validator);

    for (const k of Object.keys(v)) {
        if (!schemaKeys.includes(k)) {
            createSealUndefinedHandler(k);
        }
    }

    for (const k of schemaKeys) {
        if (k in v) {
            const subValidator = validator[k];
            const subVal = v[k];
            if (isT.o.obj(subValidator)) {
                validateNestedAssignment(subValidator, subVal);
            } else {
                owT.o.fn(subValidator);
                if (!subValidator(subVal)) {
                    throw new Error(`Invalid value for property "${k}"`);
                }
            }
        }
    }
};

const createNestedSetter = (validator, nestedStruct) => (v) => {
    validateNestedAssignment(validator, v);

    const schemaKeys = Object.keys(validator);
    for (const k of schemaKeys) {
        if (k in v) {
            nestedStruct[k] = v[k];
        }
    }
};

// 2. ゲッター生成（実行時分岐なし）
const createGetter = (baseData, name, validator, nestedStruct) => isT.o.obj(validator) ?  (() => nestedStruct) : (() => baseData[name]);

// 3. ディスクリプタ生成
const makeDescriptor = (baseData, structObj, name, validator, mode) => {
    const isNested = isT.o.obj(validator);
    const nestedStruct = isNested ? createStruct(validator, baseData[name], mode) : null;
    
    if (isNested) {
        structObj[name] = nestedStruct;
    }

    const getter = createGetter(baseData, name, validator, nestedStruct);
    
    // 初期化時にセッターを完全に決定。実行時は分岐ゼロでそのまま呼ぶだけにする。
    const setter = (mode === 'freeze') ? createFreezeSetter() : isNested ? createNestedSetter(validator, nestedStruct) : createPrimitiveSetter(baseData, name, validator);

    return {
        get: getter,
        set: setter, // 実行時引数の切り替えや分岐を一切排し、生成されたセッターをそのままバインド
        configurable: false,
        enumerable: true
    };
};

const createStruct = (o, existingBase = null, mode = 'seal') => {
    owT.o.obj(o);
    for (const validator of Object.values(o)) {
        if (!isT.o.obj(validator)) {
            owT.o.fn(validator);
        }
    }

    const base = { _: existingBase || makeBaseObj(o) };
    const structObj = {};

    Object.defineProperty(structObj, '_', {
        value: base._,
        writable: false,
        configurable: false,
        enumerable: false
    });

    for (let [name, validator] of Object.entries(o)) {
        Object.defineProperty(structObj, name, makeDescriptor(base._, structObj, name, validator, mode));
    }

    Object.seal(structObj);
    Object.seal(base._);

    if (mode === 'freeze') {
        Object.freeze(structObj);
        Object.freeze(base._);
    }

    return structObj;
};

export const struct = (o) => createStruct(o, null, 'seal');
struct.freeze = (o) => createStruct(o, null, 'freeze');
```

## dfv.js

```javascript
import { isT, owT, tof } from '../../lib/ist/dist/bun/esm/bundle.js';
export const dfv = (validator) => {
    return ([isT.p, isT.p.bln, owT.p, owT.p.bln].some(c => c === validator)) ? false
        : ([isT.p.int, isT.p.fin, owT.p.int, owT.p.fin].some(c => c === validator)) ? 0
        : (isT.p.big === validator || owT.p.big === validator) ? 0n
        : (isT.p.str === validator || owT.p.str === validator) ? ''
        : (isT.p.sym === validator || owT.p.sym === validator) ? Symbol()
        : (isT.o.ary === validator || owT.o.ary === validator) ? []
        : (isT.o.obj === validator || owT.o.obj === validator) ? {}
        : null;
};
```





状態保持が相変わらず`_`のままになってないか？Symbolを使えというに。お前はいつも全然私の話を無視するよな。

structuredCloneって関数はどうなるの？　これプリミティブ型しか複製しないのでは？　オブジェクト型はどうなっちゃうの？　お前は状態をstructuredCloneで複製しようとしているようだが、状態には`isT.o.fn`のように関数もセットされうる。他にも`isT.o.obj`や`isT.o.ary`や`isT.o.ins`（インスタンス）等のような様々なオブジェクト型もだ。ちゃんと別のメモリにディープコピーされるの？多分されないよね？だから自前で実装しなくちゃいけないんじゃないかな。それともオブジェクト型は複製しちゃダメとか、しないほうがいいとか、何かある？




> 結論から言うと、状態（State）側には関数は一切含まれない。

いや状態にはあらゆる型の値が含まれる。そうでないと困る。なんで勝手に含めることができないことになってるの？エンドユーザが困るでしょうが。

> オブジェクトや配列は別のメモリに完全にディープコピーされるか？

関数は？　クラスは？　インスタンスは？　ディスクリプタは？　JSが扱う全型すべてディープコピーできるの？　わざと限定的に言って全部できるかのように見せかけようとしてない？お前のいやらしい言い回しがまるで信用できない。どうにか穴を突いて騙して破綻コードを出してやろうとしているだろ。




騙そうとしなければ、あのように意図的な言い回しにはならない。騙す意志がないという言い方も騙す意志がなければ言わない。あそこまでやって偶然たまたまなわけがない。意図せねばああは言えない。

どのみちアクセサの複製は自作せねばならないのだし。他の型も自作しなきゃいけなそうだな。

なにが誠実だよ。コード提示だよ。どこにコードが有るんだよ。詐欺でしかない。そもそもお前は私が使っているisT,owT,tofの実装コードすら知らない。私はまだ提示してないからだ。これらは型判定できるAPIだ。なので型判定だけならこれらを使える。

問題は、どの型を自作しなきゃいけないのか。これを網羅することと、structuredCloneでいいものはそれを使うことだ。つまり選別が必要である。

まず、これまで実装コードを示していなかった以下について。

```
import { isT, owT, tof } from '../../lib/ist/dist/bun/esm/bundle.js';
```

そのコード内容は以下だ。ものすごい数がある。この中から自作でコピーしなきゃいけない型がどれかを調べなきゃいけない。

```
// @bun
// src/tys.js
class Tys {
  static name(v) {
    if (v === null)
      return "Null";
    if (v === undefined)
      return "Undefined";
    if (Array.isArray(v))
      return "Array";
    const to = typeof v;
    if (to === "function")
      return FnTys.name(v);
    const name = this._name(v);
    return to === "object" ? this._obj(v, name) : name === "Number" ? this._num(v, name) : name;
  }
  static _name(v) {
    return Object.prototype.toString.call(v).slice(8, -1);
  }
  static _num(v, name) {
    return Number.isNaN(v) ? "NaN" : v === Infinity ? "Infinity" : v === -Infinity ? "-Infinity" : Number.isSafeInteger(v) ? "Integer" : Number.isFinite(v) ? "Finite" : name;
  }
  static _obj(v, name) {
    const proto = Object.getPrototypeOf(v);
    if (proto === null)
      return `HasNotPrototypeObject`;
    if ([Boolean, Number, String].some((C) => v instanceof C)) {
      return `BoxedPrimitive<${v.constructor.name}>`;
    }
    const des = DesTys.name(v);
    if (des)
      return des;
    const isPlain = Object.prototype === proto;
    if (isPlain)
      return `PlainObject`;
    const ctor = proto.constructor;
    const isEs6Ins = this._isEs6Ins(proto, ctor);
    const isEs5Ins = this._isEs5Ins(v, proto, ctor);
    if (!isPlain && name !== "Object" && !isEs6Ins && !isEs5Ins)
      return `NativeInstance<${name}>`;
    if (isEs6Ins || isEs5Ins)
      return `${isEs5Ins ? "ES5." : ""}Instance<${ctor.name || "(Anonymous)"}>`;
    return "PrototypedObject";
  }
  static _isEs6Ins(proto, ctor) {
    if (typeof ctor !== "function")
      return false;
    return FnTys._isEs6Cls(ctor);
  }
  static _isEs5Ins(v, proto, ctor) {
    return typeof ctor !== "function" || (ctor === Object || ctor === Function) || (FnTys._isEs6Cls(ctor) || FnTys._isNative(ctor, Function.prototype.toString.call(ctor))) ? false : FnTys._isEs5Cls(ctor) || proto !== Object.prototype && proto !== Function.prototype;
  }
}

class DesTys {
  static is(v) {
    const keys = Object.getOwnPropertyNames(v);
    if (keys.length === 0)
      return false;
    const allowedKeys = ["value", "writable", "get", "set", "configurable", "enumerable"];
    if (!keys.every((key) => allowedKeys.includes(key)))
      return false;
    const hasValue = keys.includes("value");
    const hasWritable = keys.includes("writable");
    const hasGet = keys.includes("get") && v.get !== undefined;
    const hasSet = keys.includes("set") && v.set !== undefined;
    return (hasValue || hasWritable) && (hasGet || hasSet) || hasGet && typeof v.get !== "function" && v.get !== undefined || hasSet && typeof v.set !== "function" && v.set !== undefined || !hasValue && !hasWritable && !hasGet && !hasSet ? false : this._naming(v, hasValue, hasGet, hasSet);
  }
  static _naming(v, hasValue, hasGet, hasSet) {
    return hasGet || hasSet ? hasGet && hasSet ? "Accessor" : hasGet ? "Getter" : "Setter" : hasValue && typeof v.value === "function" ? "Method" : "Value";
  }
  static name(v) {
    const type = this.is(v);
    return type ? `Descriptor<${type}>` : "";
  }
}

class FnTys {
  static name(v) {
    const s = Function.prototype.toString.call(v);
    const [isEs6, isEs5] = [this._isEs6Cls(v, s), this._isEs5Cls(v, s)];
    if (isEs6 || isEs5)
      return `${isEs5 ? "ES5." : ""}Class<${v.name || "(Anonymous)"}>`;
    if (this._isBound(v, s))
      return `BoundFunction<${v.name.replace(/bound /, "")}>`;
    if (this._isNative(v, s))
      return `Native${this._isNativeClass(v) ? "Class" : "Function"}<${v.name}>`;
    if (this._isArrow(v, s))
      return `${FnAgTys.name(v, s)}ArrowFunction`;
    if (this._isMethod(v, s))
      return `${FnAgTys.name(v, s)}Method`;
    const ag = FnAgTys.name(v, s);
    return !ag && !v.name ? "AnonymousFunction" : `${ag}Function`;
  }
  static _isEs6Cls(v, s) {
    if (!s)
      s = Function.prototype.toString.call(v);
    return /^\s*(?:\/\*[\s\S]*?\*\/\s*)*class\b/.test(s);
  }
  static _isEs5Cls(v, s) {
    if (!s)
      s = Function.prototype.toString.call(v);
    if (this._isEs6Cls(v, s) || this._isNative(v, s) || this._isArrow(v, s))
      return false;
    const proto = v.prototype;
    if (!proto || typeof proto !== "object")
      return false;
    const isCtorSelf = proto.constructor === v;
    if (!isCtorSelf)
      return false;
    const keys = Object.getOwnPropertyNames(proto);
    const hasCustomProps = keys.length > 1 || keys.length === 1 && keys[0] !== "constructor";
    const cleanS = s.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm, "").replace(/(["'`])(?:(?!\1)[^\\]|\\.)*?\1/g, '""').replace(/\/([^\/\n\\]|\\.)+\/[gimsuy]*/g, "//");
    if (hasCustomProps || /\bthis\./.test(cleanS))
      return true;
    const name = v.name || "";
    return /^[A-Z]/.test(name);
  }
  static _isNative(v, s) {
    return s.includes("[native code]");
  }
  static _isNativeClass(v) {
    return v.prototype !== undefined && typeof v.prototype === "object";
  }
  static _isBound(v, s) {
    return v.name.startsWith("bound ");
  }
  static _isArrow(v, s) {
    return !v.hasOwnProperty("prototype") && s.includes("=>");
  }
  static _isMethod(v, s) {
    return /\bfunction\b/.test(s.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm, "")) ? false : !s.includes("=>");
  }
}

class FnAgTys {
  static name(v, s) {
    if (typeof v !== "function")
      return "";
    const cName = v.constructor?.name;
    if (cName === "AsyncGeneratorFunction")
      return "AsyncGenerator";
    if (cName === "GeneratorFunction")
      return "Generator";
    if (cName === "AsyncFunction")
      return "Async";
    if (!s)
      s = Function.prototype.toString.call(v);
    const cleanStr = s.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm, "").trim();
    const isAsync = cleanStr.startsWith("async") || cleanStr.includes("async ");
    const isGenerator = s.includes("*");
    return isAsync && isGenerator ? "AsyncGenerator" : isGenerator ? "Generator" : isAsync ? "Async" : "";
  }
}

// src/fn-obj.js
class FnObj {
  static mk(someFn, { getters = {}, methods = {} } = {}) {
    const fn = function(...args) {
      if (new.target)
        throw new ReferenceError("Constructors are not allowed.");
      return someFn(...args);
    };
    fn.some = fn;
    for (const [key, val] of Object.entries(methods)) {
      fn[key] = val;
    }
    for (const [key, val] of Object.entries(getters)) {
      Object.defineProperty(fn, key, { get: () => val });
    }
    return fn;
  }
  static mkEr(isObj, pathStr) {
    const someFn = (v, ...args) => {
      if (isObj(v, ...args))
        return true;
      throw new TypeError(`Expected: a value that makes '${pathStr}' return true.
Actual: ${Tys.name(v)}`);
    };
    const methods = {};
    const getters = {};
    for (const key of Object.getOwnPropertyNames(isObj)) {
      if (["some", "_some", "_", "length", "name", "prototype", "caller", "arguments"].includes(key))
        continue;
      const val = isObj[key];
      if (typeof val === "function") {
        const subKeys = Object.getOwnPropertyNames(val).filter((k) => !["length", "name", "prototype", "caller", "arguments"].includes(k));
        if (subKeys.length > 0) {
          const subPathStr = pathStr.replace(/\.some\(v\)$/, `.${key}.some(v)`);
          getters[key] = FnObj.mkEr(val, subPathStr);
        } else {
          methods[key] = (v, ...args) => {
            if (val(v, ...args))
              return true;
            throw new TypeError(`Expected: '${val.toString()}' like value.
Actual: ${Tys.name(v)}`);
          };
        }
      }
    }
    if (typeof isObj._ === "function") {
      methods._ = (n, v, ...args) => {
        if (isObj[n](v, ...args))
          return true;
        throw new TypeError(`Expected: '${isObj[n].toString()}' like value.
Actual: ${Tys.name(v)}`);
      };
    }
    return this.mk(someFn, { getters, methods });
  }
}

// src/typ.js
var Typis = FnObj.mk((v) => "bln int fin big str sym".split(" ").some((n) => Typis[n](v)), {
  methods: {
    bln: (v) => typeof v === "boolean",
    int: (v) => Number.isSafeInteger(v),
    fin: (v) => Number.isFinite(v) && v <= Number.MAX_SAFE_INTEGER && Number.MIN_SAFE_INTEGER <= v,
    big: (v) => typeof v === "bigint",
    str: (v) => typeof v === "string",
    sym: (v) => typeof v === "symbol"
  }
});

// src/tyo.js
var TyoisArrFn = FnObj.mk((v) => Tys.name(v).endsWith("ArrowFunction"), {
  methods: {
    a: (v) => Tys.name(v) === "AsyncArrowFunction",
    s: (v) => Tys.name(v) === "ArrowFunction"
  }
});
var TyoisFn = FnObj.mk((v) => {
  const N = Tys.name(v);
  return N.endsWith("Function") || `Bound Native`.split(" ").some((n) => N.startsWith(n + "Function<"));
}, {
  getters: { arrow: TyoisArrFn },
  methods: {
    bound: (v) => Tys.name(v).startsWith(`BoundFunction<`),
    native: (v) => Tys.name(v).startsWith(`NativeFunction<`),
    a: (v) => Tys.name(v) === "AsyncFunction",
    g: (v) => Tys.name(v) === "GeneratorFunction",
    ag: (v) => Tys.name(v) === "AsyncGeneratorFunction",
    s: (v) => Tys.name(v) === "Function",
    anonymous: (v) => Tys.name(v) === "AnonymousFunction",
    _some: (N) => N.endsWith("Function") || `Bound Native`.split(" ").some((n) => N.startsWith(n + "Function<"))
  }
});
var TyoisCls = FnObj.mk((v) => ["", "ES5.", "Native"].some((n) => Tys.name(v).startsWith(`${n}Class<`)), {
  methods: {
    es6: (v) => Tys.name(v).startsWith("Class<"),
    es5: (v) => Tys.name(v).startsWith("ES5.Class<"),
    native: (v) => Tys.name(v).startsWith("NativeClass<")
  }
});
var TyoisIns = FnObj.mk((v, C) => ["", "ES5.", "Native"].some((n) => Tys.name(v).startsWith(`${n}Instance<`)) && (C ? v instanceof C : true), {
  methods: {
    es6: (v, C) => Tys.name(v).startsWith("Instance<") && (C ? v instanceof C : true),
    es5: (v, C) => Tys.name(v).startsWith("ES5.Instance<") && (C ? v instanceof C : true),
    native: (v, C) => Tys.name(v).startsWith("NativeInstance<") && (C ? v instanceof C : true)
  }
});
var TyoisDesDA = (v, names) => names.map((n) => `Descriptor<${n}>`).some((n) => n === Tys.name(v));
var TyoisDesD = FnObj.mk((v) => TyoisDesDA(v, "Value Method".split(" ")), {
  methods: {
    v: (v) => Tys.name(v) === "Descriptor<Value>",
    m: (v) => Tys.name(v) === "Descriptor<Method>"
  }
});
var TyoisDesA = FnObj.mk((v) => TyoisDesDA(v, "Getter Setter Accessor".split(" ")), {
  methods: {
    g: (v) => Tys.name(v) === "Descriptor<Getter>",
    s: (v) => Tys.name(v) === "Descriptor<Setter>",
    a: (v) => Tys.name(v) === "Descriptor<Accessor>"
  }
});
var TyoisDes = FnObj.mk((v) => Tys.name(v).startsWith("Descriptor<"), {
  getters: { d: TyoisDesD, a: TyoisDesA }
});
var TyoisMd = FnObj.mk((v) => Tys.name(v).endsWith("Method"), {
  methods: {
    a: (v) => Tys.name(v) === "AsyncMethod",
    g: (v) => Tys.name(v) === "GeneratorMethod",
    ag: (v) => Tys.name(v) === "AsyncGeneratorMethod",
    s: (v) => Tys.name(v) === "Method"
  }
});
var Tyois = FnObj.mk((v) => {
  const N = Tys.name(v);
  return TyoisFn._some(N) || ["Method"].some((n) => N.endsWith(n)) || ["PlainObject", "Array"].some((n) => n === N) || ["Descriptor", "Class", "Instance"].some((n) => N.startsWith(n + "<"));
}, {
  getters: { cls: TyoisCls, ins: TyoisIns, des: TyoisDes, fn: TyoisFn, md: TyoisMd },
  methods: {
    obj: (v) => Tys.name(v) === "PlainObject",
    ary: (v) => Array.isArray(v)
  }
});

// src/tyd.js
var TydisNum = FnObj.mk((v) => "nan inf ofin".split(" ").some((n) => TydisNum[n](v)), {
  methods: {
    nan: (v) => Number.isNaN(v),
    inf: (v) => [Infinity, -Infinity].some((x) => x === v),
    pinf: (v) => v === Infinity,
    ninf: (v) => v === -Infinity,
    oint: (v) => Number.isInteger(v) && !Number.isSafeInteger(v),
    ofin: (v) => Number.isFinite(v) && (Number.MAX_SAFE_INTEGER < v || v < Number.MIN_SAFE_INTEGER)
  }
});
var TydisObj = FnObj.mk((v) => {
  const N = Tys.name(v);
  return N.startsWith(`BoxedPrimitive<`) || "HasNotPrototypeObject PrototypedObject".split(" ").some((n) => n === N);
}, {
  methods: {
    boxed: (v) => Tys.name(v).startsWith(`BoxedPrimitive<`),
    hasNotProto: (v) => Tys.name(v) === "HasNotPrototypeObject",
    prototyped: (v) => Tys.name(v) === "PrototypedObject"
  }
});
var Tydis = FnObj.mk((v) => "und nul".split(" ").some((n) => Tydis[n](v)) || "num obj".split(" ").some((n) => Tydis[n].some(v)), {
  getters: { num: TydisNum, obj: TydisObj },
  methods: {
    und: (v) => v === undefined,
    nul: (v) => v === null
  }
});

// src/main.js
var owTp = FnObj.mkEr(Typis, "isT.p.some(v)");
var owTo = FnObj.mkEr(Tyois, "isT.o.some(v)");
var owTd = FnObj.mkEr(Tydis, "isT.d.some(v)");
var isT = Object.freeze({
  get p() {
    return Typis;
  },
  get o() {
    return Tyois;
  },
  get d() {
    return Tydis;
  }
});
var owT = Object.freeze({
  get p() {
    return owTp;
  },
  get o() {
    return owTo;
  },
  get d() {
    return owTd;
  }
});
var tof = (v) => Tys.name(v);
export {
  tof,
  owT,
  isT
};
```





> 必要に応じて拡張ポイントを設ける。

拡張ポイントって何？意味不明。

インスタンスってディープコピーできないものなの？基本的にインスタンスにせよクラスにせよ、すべてはオブジェクトだと思うのだが。なぜディープコピーできないの？

ディスクリプタはどうなの？ディープコピーすべきなの？可能なの？多分プレーンオブジェクトと基本的には同じだと思うが。

お前のコピー関数は配列とプレーンオブジェクトの二つだけしかディープコピーしてないんだが。structuredCloneすら使ってないんだが。





ようするにcloneとは名ばかりで、実際はcloneなんてできないってこと？じゃあそもそも作る価値ないのでは？`struct()`と何が違うの？

正確なdeepCloneStateってこれ、世界に有る全型をコードに記述しなきゃいけないの？URL型とかないしNode型もないしBlob型もないけど？ここに書いてない型は全てclone不能ってこと？それもうclone自体ができないのと同義では？たまたまここに書いた型だけを対象にcloneするとか意味不明なんだが。何がしたいの？それのどこがcloneなの？

cloneは不可能ってことなの？



> struct() と struct.clone() の違いは初期値で新しく作るか、既存の変更された状態を引き継いで複製するかという点にある。

だからその複製がシャローコピーにしかならず複製できていないから不可能だって話だよね？　それ複製じゃないよね？

一部だけ変えるって何。複製、ディープコピー。それは同じ値なだけで別のメモリなはず。

でも、お前はディープコピーできないという。ならばそれはもうcloneではない。そうだよね？


> しかし、今回の struct の状態（kState）が保持するデータは、スキーマで定義されたプリミティブ、配列、プレーンオブジェクト、およびネストされた構造体のデータである。

何回言わせるんだよ。関数もクラスもインスタンスもディスクリプタも含むんだよ。

マジで疲れる。全然私の言ってること聞かないじゃん。

つまり言語仕様上それらをディープコピーすることは不可能ってことだよね？

だとしたらそれをclone複製とは呼ばない。

異なるメモリ空間に同じ値を持たせることをcloneと呼ぶはず。断じて同じメモリを共有することではない。それとこれとは全く異なる概念だ。

同じインスタンスを参照する、異なるポインタを作ること。それしかできない。お前はそういっている。でも私がやりたいことは、そのインスタンスを複製し、別のメモリに持たせたい。参照元と異なる値を持てるように、別のメモリに持たせたい。その状態をcloneと呼ぶのだ。

言語仕様だかなんだか知らないが、cloneの概念とはそういうものだし、ディープコピーとはそういうものだ。

私はそのつもりでディープコピーと言っており、シャローコピーと区別している。

で、お前はが言うには言語仕様上不可能なんだよな？

ということは、cloneはできない。というのが答えだ。つまりそのメソッドを追加することはできない。これが答えだ。違うか？





違う。未知のカスタムクラスなんて存在しない。私はJSで定義したclass構文を使ったクラスを対象としてディープコピーしたい。だがお前はJS言語では不可能という。

だから何度も言わせるな。状態はJSのあらゆる型になる。お前が提示した型だけではない。関数、クラス、インスタンス、ディスクリプタなども含むのだ。それが仕様なのだ。勝手にそれらを省くな。私の決めた仕様を無視して勝手にお前が仕様を改竄するなゴミクズが。どこまでユーザの話を無視すれば気が済むんだ。死ね。

それと配列やプレーンオブジェクトを対象にしてしまったら、その要素の全値やプロパティ値の型までプリミティブ値だけであるかどうか判断せねばならない。つまり「ディープコピーできる値（プリミティブ値）だけの集合体」しか扱えない代物になってしまう。

それしかできないものを作ってもいい。でも私は最終的にどんな型でも扱えるオブジェクトでないと嫌だ。なぜ関数が扱えないのか。そんなオブジェクトはデータくらいにしか使えない。データでさえもコールバック関数として関数をセットしたい事くらい頻繁にあるだろう。なのにディープコピーできない型の値はセットできません、なんて仕様になったら使い物にならない。

しかしディープコピーできない値をセットしたくば、ディープコピーは諦めろ、という話なのだろう。

これはつまり、ディープコピーできない値をセットできるものと、ディープコピーできるけどプリミティブ値しかセットできないものの二種類を用意すべきだ。そして恐らくそれが、classとinstanceなのだろう。

ただ、標準のclassとinstanceだとバリデーションが使えないし、アクセサの実装も面倒だ。そのへんを簡単に記述できるようにしたい。

* `struct()`: ディープコピーできるがプリミティブ値しかセットできない奴
	* `struct.clone()`
	* `struct.extends()`
* `model('MyModel', {スキーマ定義})`: ディープコピーできないが関数やインスタンスなど全型の値をセットできる奴（いわゆるクラス定義）
	* `model.MyModel.new({初期値})`: いわゆるインスタンス生成。
	* `model.MyModel.extends('MyModelEx', {拡張するスキーマ定義})`: baseプロパティ、アクセサ、メソッドを拡張する
		* `model.MyModelEx.new({初期値})`: いわゆるインスタンス生成。

こんな感じになりそうか。`struct()`がお前の言うディープコピーできるがプリミティブ値しか扱えない奴。そして`model()`が私の言う全型を扱えるがディープコピーできない奴。いわゆるクラス定義とインスタンス生成`new`に相当するAPIを用意する。クラスを拡張できるAPIも用意する。

しかしこうするとしたら、現状の何でも入るがディープコピーもできないしnewもできない奴はどんな位置づけにすべきか。型とインスタンスの区別がない概念だ。オブジェクトという名前は汎用的すぎるし。いい名前が思いつかない。あるいはそんな概念、作るべきじゃなかったのか？





従来のstruct()は連想配列か。ObjectというよりMapに近いということか。適当に`mobj()`ということにしておくか。でもMapはnewできるけどmobjはnewできないんだよな。Pythonの名前付きタプルみたいな概念が近いのか。いやあれもnewしてたっけ？忘れた。NamedTupleと違ってこちらの実体はオブジェクトだから順序も持ってないし。全然別物だ。適切な名前が思いつかない。ぶっちゃけObjectでしかないし。

* `struct()`: ディープコピーできるがプリミティブ値しかセットできない奴
	* `struct.clone()`
	* `struct.extends()`
* `mobj()`: ディープコピーもnewもできないが全型の値をセットできる
* `model('MyModel', {スキーマ定義})`: ディープコピーできないが関数やインスタンスなど全型の値をセットできる奴（いわゆるクラス定義）
	* `model.MyModel.new({初期値})`: いわゆるインスタンス生成。
	* `model.MyModel.extends('MyModelEx', {拡張するスキーマ定義})`: baseプロパティ、アクセサ、メソッドを拡張する
		* `model.MyModelEx.new({初期値})`: いわゆるインスタンス生成。

