# TypedModel

* https://github.com/ytyaru/JS.Typ.20260823112517

これを使い型判定するプロパティを持つオブジェクトを生成する。

```javascript
const human = model({
  name: isT.p.str,
  age: isT.p.int,
  items: isT.o.ary,
  children: {
    sub: isT.o.obj,
  }
});
```

Object.defineProperty(o, name, {get(){} set(v){}})


```
structure-object
class-instance
model-entity
template-document
```


パターン1は助かった。しかしパターン2は1より1字長くなっており短くなっていないし、パターン3に至ってはvalidatorの結果に関わらず代入してしまうため元のコードと挙動が変わりバグるのでは？


それ以外は元のコードと挙動が変わってしまいバグるのでは？



[isT.p, isT.p.bln, owT.p, ow.p.bln].some(c=>c===v)

    if ([isT.p.int,isT.p.fin,owT.p.int,owT.p.fin].some(c=>c===v)) return 0;
    if (isT.p.big===v || owT.p.big===v) return 0n;
    if (isT.p.str===v || owT.p.str===v) return '';
    if (isT.p.sym===v || owT.p.sym===v) return Symbol();
    if (isT.o.ary===v || owT.o.ary===v) return [];
    if (isT.o.obj===v || owT.o.obj===v) return {};
    return null;

